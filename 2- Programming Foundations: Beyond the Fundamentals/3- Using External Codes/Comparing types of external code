->Python module:
    python file that contains code, like variables or functions

->Package or Library:
    using multiple modules together so they are distributed and used
    in a group

->Framework:
    when a set of code is not just used together but used in a 
    specific way